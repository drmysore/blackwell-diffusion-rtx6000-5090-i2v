type: object
required:
  - step_type
  - prompt
  - version
properties:
  step_type:
    type: string
    enum:
      - headshot
      - dataset
      - content
  prompt:
    type: string
    minLength: 10
  version:
    type: string
  description:
    type: string
    description: Optional description of changes
