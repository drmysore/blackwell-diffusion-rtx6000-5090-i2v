type: object
properties:
  visibility:
    type: string
    enum:
      - public
      - connections
    default: public
  shareType:
    type: string
    enum:
      - article
      - image
      - video
      - document
